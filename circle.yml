machine:
  java:
    version: oraclejdk8
dependencies:
  override:
    - mkdir -p ~/.bintray && envsubst < .credentials > ~/.bintray/.credentials
test:
  override:
    - sbt test
deployment:
  staging:
    branch: master
    commands:
      - sbt "set version := \"0.0.1-$(git rev-parse HEAD)\"" package publish
#  release:
#    branch: /release-.*/
#    commands:
#      - sbt publish
